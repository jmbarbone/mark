
# profile environment
# .pe <- new.env()
# .pe$op <- options()
#
# .ResetOptions <- function() {
#   on.exit(options(.pe$op))
#   invisible(NULL)
# }

# Option preferences
options(tidyverse.quiet = TRUE)

# Comment out when building jordan
# jordan::.LoadFunctionsFromJordan()
# jordan::.RunDefaultFunctionsFromJordan()
