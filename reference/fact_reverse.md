# Fact reverse levels

Reverse the levels of a `fact`

## Usage

``` r
fact_reverse(x)
```

## Arguments

- x:

  A `fact` object (or passed to
  [`fact()`](https://jmbarbone.github.io/mark/reference/fact.md))
