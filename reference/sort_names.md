# Sort by names

Sort a vector by it's name

## Usage

``` r
sort_names(x, numeric = FALSE)
```

## Arguments

- x:

  A named vector of values

- numeric:

  If `TRUE` will try to coerce to numeric

## Value

`x` sorted by its [`names()`](https://rdrr.io/r/base/names.html)
